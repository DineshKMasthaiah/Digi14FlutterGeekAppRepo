import 'package:flutter/material.dart';
///Colors used or going to use in the application
class GEColors {
  static const Color gray = Color.fromRGBO(112, 112, 112, 1.0);
  static const Color black = Colors.black;
  static const Color screenBackground = Colors.white;
  static const Color white = Colors.white;
  static const Color darkBlue = Color(0xFF003A6E);
  static const Color semiDarkGray = Color.fromRGBO(141, 145, 148, 1.0);
  static const Color listItem2ndLine = Colors.grey;
  static const Color primaryButtonColor = Color.fromRGBO(0, 113, 185, 1.0);
}
